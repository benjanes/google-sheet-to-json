{
  "name": "google-sheet-to-json",
  "version": "1.0.0",
  "description": "Convert a google sheet into readable JSON without the overhead of the official API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ben Janes",
  "license": "ISC"
}
