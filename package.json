{
  "name": "google-sheet-to-json",
  "version": "1.0.0",
  "description": "Convert a shared google sheet into JSON without the official API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ben Janes",
  "license": "ISC",
  "dependencies": {
    "request": "^2.79.0"
  }
}
